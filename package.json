{
  "name": "sawt",
  "version": "0.5.0",
  "private": true,
  "description": "Audio-first language learning apps: point at a thing, hear its name, then guess it by ear",
  "license": "MIT",
  "author": "Amer Harb <me@amerharb.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/amerharb/sawt"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "typecheck": "npm exec --workspaces -- tsc --noEmit",
    "dev": "echo \"pick an app: npm run dev -w <week|flags|colors|arqaam|anthem>\" && exit 1",
    "preview": "echo \"pick an app: npm run preview -w <week|flags|colors|arqaam|anthem>\" && exit 1"
  },
  "engines": {
    "node": ">=20.19"
  }
}
