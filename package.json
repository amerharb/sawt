{
  "name": "sawt",
  "version": "0.22.0",
  "private": true,
  "type": "module",
  "description": "Audio-first language learning apps: point at a thing, hear its name, then guess it by ear",
  "license": "MIT",
  "author": "Amer Harb <me@amerharb.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/amerharb/sawt"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm exec --workspaces -- tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "dev": "echo \"pick an app: npm run dev -w <home|week|flag|color|numbers|anthem|face>\" && exit 1",
    "preview": "echo \"pick an app: npm run preview -w <home|week|flag|color|numbers|anthem|face>\" && exit 1"
  },
  "engines": {
    "node": ">=20.19"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.9.0",
    "typescript-eslint": "^8.66.0"
  }
}
